server:
  port: 8084
  address: 0.0.0.0

spring:
  application:
    name: alert-service
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://34.66.242.149:5432/alert_service_db}
    username: ${SPRING_DATASOURCE_USERNAME:alert_user}
    password: ${SPRING_DATASOURCE_PASSWORD:leon1611}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.petnow.alert: DEBUG 